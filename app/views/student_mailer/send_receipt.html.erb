<h1>New receipt from Aidah Manaar</h1>

<ul>
  <% @invoice.items.each do |item| %>
    <li><%= item.name %> — <%= number_to_currency(item.price) %></li>
  <% end %>
  <li>
    <hr>
  </li>
  <li>Total — <%= number_to_currency(@invoice.items.map(&:price).sum) %></li>
</ul>
