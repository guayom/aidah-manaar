<nav class="navbar navbar-default" data-spy="affix">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
              data-target="#navbar-collapse-1" aria-expanded="false">
        <span class="sr-only"><%= t('views.navigation.toggle') %></span>
        <spab class="icon-bar"></spab>
        <spab class="icon-bar"></spab>
        <spab class="icon-bar"></spab>
      </button>
      <a class="navbar-brand" href="/">Aidah Manaar</a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse-1">
      <%= render_navigation %>

      <ul class="nav navbar-nav navbar-right">
        <% if student_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
               aria-haspopup="true" aria-expanded="false">
              <%= current_student.email %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Profile', current_student %>
              </li>
              <li>
                <%= link_to t('devise.sign_out'), destroy_student_session_path, method: :delete %>
              </li>
            </ul>
          </li>
      <% else %>
          <li class="<%= 'active' if current_page?(new_student_session_path) %>">
            <%= link_to t('devise.sign_in'), new_student_session_path %>
          </li>

          <li class="<%= 'active' if current_page?(new_student_registration_path) %>">
            <%= link_to t('devise.sign_up'), new_student_registration_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<div class="affix-margin"></div>
